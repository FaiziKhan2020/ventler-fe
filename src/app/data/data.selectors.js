export const selectSelectedFiles = (state) => state.data.selectedFiles;

export const selectFileFolders = (state) => state.data.fileFolders;

export const selectChats = (state) => state.data.chats;

export const selectFlows = (state) => state.data.flows;

export const selectMessages = (state) => state.data.messages;
